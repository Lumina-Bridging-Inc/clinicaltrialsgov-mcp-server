{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["bun install --frozen-lockfile --ignore-scripts"]
    },
    "build": {
      "commands": ["bun ./scripts/railway-build.ts"]
    }
  },
  "deploy": {
    "startCommand": "bun run dist/index.js"
  }
}
